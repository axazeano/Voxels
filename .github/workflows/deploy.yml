name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # Сборки нет: страницы статические. В корень сайта кладём WebGPU-версию,
      # CPU-версия доступна как cpu.html, оригинальные имена тоже сохраняются.
      - run: |
          mkdir -p site
          cp webgpu.html site/index.html
          cp index.html site/cpu.html
          cp webgpu.html index.html README.md site/ 2>/dev/null || true
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
